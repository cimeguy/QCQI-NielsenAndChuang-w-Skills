name: Deploy QCQI site to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "skill_set/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Disable Jekyll (serve _viz and other underscore paths as-is)
        run: touch skill_set/.nojekyll
      - uses: actions/upload-pages-artifact@v3
        with:
          path: skill_set
      - id: deployment
        uses: actions/deploy-pages@v4
