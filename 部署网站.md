# 部署网站操作手册

把 `skill_set/` 下的知识库静态站点发布到 GitHub Pages 的完整人工操作流程。

线上地址：<https://cimeguy.github.io/QCQI-NielsenAndChuang-w-Skills/>

---

## 0. 一次性准备（只做一次）

### 0.1 确认远程仓库
本仓库有两个 remote，**只能推 `cimeguy`，绝不要推 `origin`**：

```bash
git remote -v
# cimeguy  git@github.com:cimeguy/QCQI-NielsenAndChuang-w-Skills.git   ← 推这个
# origin   https://github.com/Katakluzein/QCQI-NielsenAndChuang.git    ← 别碰
```

### 0.2 在 GitHub 上开启 Pages（关键，漏了会部署失败）
1. 打开仓库 `cimeguy/QCQI-NielsenAndChuang-w-Skills` 的 **Settings → Pages**
2. **Build and deployment → Source** 选择 **GitHub Actions**（不是 "Deploy from a branch"）
3. 保存即可，不需要选分支

> 只有把 Source 设成 GitHub Actions，`.github/workflows/pages.yml` 才能成功部署。
> 第一次如果忘了设置，工作流会报错：`Get Pages site failed...`，设置好后重跑一次即可。

---

## 1. 日常发布流程（每次更新内容都走这套）

### 1.1 改源文件
所有内容来自 `skill_set/<skill>/` 下的 Markdown：
- `SKILL.md` / `chapters/*.md` / `glossary.md` 等 → 公式精简版
- `explained.md` → 🗣️ 人话版
- `student.md` → 🎓 大学生版

### 1.2 重新生成 HTML（必须，否则线上是旧的）
```bash
cd skill_set
python3 build_html.py        # 生成 index.html（公式版）+ 各章页
python3 build_explained.py   # 生成 explained.html（人话版）
python3 build_student.py     # 生成 student.html（大学生版）
```
纯标准库，无需安装任何依赖。

> ⚠️ 常见坑：只改了 `.md` 没跑 build 脚本，或只跑了其中一个脚本，会导致线上某个版本是旧内容。三个脚本按需全跑。

### 1.3 提交并推送（从仓库根目录操作）
```bash
cd /Users/gaoli44/Desktop/quantum/QCQI-NielsenAndChuang   # 回到根目录，不要在 skill_set/ 里 commit
git add skill_set
git commit -m "update: 说明本次改了什么"
git push cimeguy main        # 只推 cimeguy
```

### 1.4 等待自动部署
推送后 GitHub Actions 会自动触发（仓库 **Actions** 标签页可看进度），
约 1~2 分钟后线上生效。也可在 Actions 页面手动 **Run workflow**（workflow_dispatch）。

---

## 2. 自动部署的触发规则

部署由 `.github/workflows/pages.yml` 控制，触发条件如下：

```yaml
on:
  push:
    branches: [main]
    paths:
      - "skill_set/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:
```

- ✅ **自动触发**：`git push cimeguy main` 时，只要这次改动**碰了 `skill_set/**` 或工作流文件本身**，就会自动跑部署，1~2 分钟后线上生效。
- ❌ **不会触发**：只改了仓库根目录文件（如单独改本文件 `部署网站.md`、根目录 `README.md`）而没碰 `skill_set/`，**不会**自动部署——因为站点内容全在 `skill_set/` 下，改外面无需重发。
- 🖐 **手动触发**：`workflow_dispatch` 允许在仓库 **Actions** 页面点 **Run workflow** 手动跑一次（想强制重新部署时用）。
- ⚙️ **前提**：必须已在 **Settings → Pages → Source** 选了 **GitHub Actions**（见 0.2），否则自动部署不生效。
- 👀 **确认结果**：到仓库 **Actions** 标签页看最新一条 “Deploy QCQI site to GitHub Pages” 是否为绿色对勾。

---

## 3. 部署原理（了解即可）

- 工作流文件：`.github/workflows/pages.yml`
- 它把整个 `skill_set/` 目录作为站点根上传部署
- 会 `touch skill_set/.nojekyll`，禁用 Jekyll，保证 `_viz/` 等下划线开头目录正常可访问
- MathJax 公式渲染走本地离线包 `skill_set/vendor/tex-svg.js`（2MB，已纳入 git，不用 CDN，离线也能渲染）

---

## 4. 排错

### 4.1 公式显示成 LaTeX 源码（没渲染）
绝大多数是**浏览器缓存**：你在「首次部署还没成功 / vendor 文件还没上传」时打开过页面，浏览器缓存了坏状态。
- 解决：硬刷新 `Cmd+Shift+R`（Mac）/ `Ctrl+F5`（Win），或用无痕窗口打开
- 验证服务端是否正常：
  ```bash
  curl -sI https://cimeguy.github.io/QCQI-NielsenAndChuang-w-Skills/vendor/tex-svg.js | head -3
  # 期望 HTTP/2 200，content-type: application/javascript
  ```
  返回 200 就说明服务端没问题，纯属本地缓存。

### 4.2 内容没更新
- 确认跑了对应的 build 脚本并 `git add skill_set` 提交了生成的 `*.html`
- 确认推到了 `cimeguy`，且 Actions 跑成功（绿色对勾）

### 4.3 Actions 报 `Get Pages site failed`
回到 **0.2**，把 Settings → Pages 的 Source 设成 **GitHub Actions**，然后重跑工作流。

---

## 5. 一句话速记
改 `.md` → 在 `skill_set/` 里跑三个 `build_*.py` → 回根目录 `git add skill_set && git commit && git push cimeguy main` → 等 Actions 跑完 → 硬刷新看效果。
